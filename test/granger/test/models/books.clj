(ns granger.test.models.books
  (:use clojure.test
        granger.models.books))

;;; once fixture (connect to db)
;;; ------------------------------------------------------------------



;;; each fixture (clear db)
;;; ------------------------------------------------------------------



;;; tests
;;; ------------------------------------------------------------------

(deftest add-test
  (is true))

(deftest update-test
  (is true))

(deftest find-test
  (is true))

(deftest find-all-test
  (is true))

(deftest is-valid-test
  (is true))